#include<stdio.h>

int main() {
    int speed;
    printf("Enter speed:");
    scanf("%d",&speed);
    
    if (speed<=80) {
        printf("Within Speed Limit");
    }
    else {
        printf("Speed Limit Exceeded");
    }
    return 0;
}