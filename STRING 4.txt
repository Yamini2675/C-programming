#include<stdio.h>

int main() {
    char str[100];
    fgets(str,sizeof(str),stdin);
    int rev=0,i=0;
    while(str[i]!='\0') {
        rev++;
        i++;
    }
    for ( i=rev-1;i>=0;i--) {
        printf("%c",str[i]);
    }
    return 0;
}