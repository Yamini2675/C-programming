#include<stdio.h>
int main() {
    int N,count=0;
    scanf("%d",&N);
    while (N>0) {
        if (N&1) {
            count++;
        }
        N=N>>1;
    }
    printf("%d",count);
    return 0;
}