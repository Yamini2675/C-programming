#include <stdio.h>

int main () {
    int str[200];
    char *p;
    int count=1;
    fgets(str,sizeof(str),stdin);
    p=str;
    for (int i=0;*(p+i)!='\0';i++) {
        if (*(p+i)==' ') {
            count++;
        }
    }
    printf("%d",count);
    return 0;

}