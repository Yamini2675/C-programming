#include<stdio.h>

int main() {
    int n,sum,rem;
    scanf("%d",&n);
    sum=0;
    for (int i=1;i<=n;n/=10) {
        rem=n%10;
        sum+=rem;
    }
    printf("%d",sum);
    return 0;
}