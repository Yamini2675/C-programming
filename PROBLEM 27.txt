#include<stdio.h>

int main() {
    int pass_len;
    printf("Enter  Password length:");
    scanf("%d",&pass_len);

    if (pass_len>=8) {
        printf("Strong password");
    }
    else {
        printf("Weak password");
    }
    return 0;
}