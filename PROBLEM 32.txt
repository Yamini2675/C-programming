#include<stdio.h>

int main() {
    int mark;
    printf("Enter mark: ");
    scanf("%d",&mark);

    if (mark>=40 && mark<=100) {
        printf("Pass");
    }
    else if (mark<40) {
        printf("Fail");
    }
    else {
        printf("Invalid mark");
    }
    return 0;
}