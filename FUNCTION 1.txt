#include<stdio.h>

float interest(int P,int R,int T) {
    int sum;
    sum=(P*R*T)/100;
    return sum;
}

int main() {
    int p,r,t;
    scanf("%d %d %d",&p,&r,&t);
    interest(p,r,t);
    printf("%.2f",interest(p,r,t));

    return 0;
}