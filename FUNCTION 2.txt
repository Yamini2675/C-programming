#include<stdio.h>

int largenum(int A,int B) {
    if (A>B) {
        printf("%d",A);
    }
    else {
        printf("%d",B);
    }
}

int main() {
    int a,b;
    scanf("%d %d",&a,&b);
    largenum(a,b);

    return 0;
}