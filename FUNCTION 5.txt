#include<stdio.h>

int sum(int n) {
    int add=0;
    while (n>0) {
        int rem=n%10;
        add=add+rem;
        n=n/10;
    }
    printf("%d",add);
}

int main() {
    int n;
    scanf("%d",&n);
    sum(n);

    return 0;
}