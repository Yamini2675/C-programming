#include<stdio.h>

int main() {
    char str[100];
    fgets(str,sizeof(str),stdin);
    int count=0;
    int word=0;
    for (int i=0;str[i]!='\0';i++) {
        if (str[i]==' ') {
            count++;
        }
        word=count+1;
    }
    printf("%d",word);
    return 0;
}